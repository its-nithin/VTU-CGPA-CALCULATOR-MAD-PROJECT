<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.CgpaCalculator"
        tools:targetApi="31">
        <activity android:name=".Conversion" />
        <activity android:name=".Calculation" />
        <activity android:name=".Eight" />
        <activity android:name=".Firstandsecondd" />
        <activity android:name=".Seventh" />
        <activity android:name=".Sixth" />
        <activity android:name=".ThirdFourth" />
        <activity android:name=".Fifth" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CgpaCalculator.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>